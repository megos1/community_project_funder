<h1>Projects</h1>
    

<%= link_to 'New Project', new_project_path %>
    
  <div class="row">

    <% @projects.each do |gold| %>
    
          <div class="col-sm-6 col-md-4 col-lg-4">
            <%= link_to(gold ) do %>

            <div class="thumbnail">
              <%= image_tag gold.image.url(:medium) %>
              <div class="caption">
              <h3><%= gold.name %></h3>
              <p><%= gold.location %></p>
              <p><%= gold.description %></p>
              <p> <%= gold.duration %></p>
              <div class="progress">
              <div class="progress-bar" role="progressbar" aria-valuenow="60" aria-valuemin="0" aria-valuemax="100" style="width: 60%;">
              <span class="sr-only">60% Complete</span>
              </div>
            </div>
            <% end %>
                <p>
                <a href="#" class="btn btn-primary" role="button">Button</a> <a href="#" class="btn btn-default" role="button">Button</a>
                <%= link_to 'Edit', edit_project_path(gold) %>
                <%= link_to 'Destroy', gold, method: :delete, data: { confirm: 'Are you sure?' } %>
                </p>
              </div>
            </div>
          </div>   
 <% end %>
 </div>




