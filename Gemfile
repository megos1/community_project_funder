source 'https://rubygems.org'

gem 'rails', '4.1.1'

gem 'jquery-rails' # Just A Query
gem 'sass-rails', '~> 4.0.3' # CSS With Attitude
gem 'bootstrap-sass' # Pull ourselves up by our
gem 'slim-rails' # Lose weight with Slim Templates
gem 'coffee-rails', '~> 4.0.0' # A Cup Of "Coffee" Script
gem 'uglifier', '>= 1.3.0' # JS Minify
gem 'turbolinks' # AJAXed Page Get
gem 'jbuilder', '~> 2.0' # JSON APIs
gem 'tzinfo-data', platforms: [:mingw, :mswin] # Rails Core Windows Dependency, Provides Time Zone Info

# Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
gem 'spring',        group: :development

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]



gem 'devise'
gem 'paperclip', github: 'thoughtbot/paperclip'

group :development do
  gem 'sqlite3'
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'guard-livereload', require: false
end

group :production do
  gem 'pg'
  gem 'rails_12factor'
end